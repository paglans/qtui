# qtui for the AMBER beamline
This code is a testing gui for interfacing to the control system at the AMBER beamline.

## Suggested installation procedure
- python -m env qtui
- source qtui/bin/activate
- python -m pip install -r requirements.txt
